# Work Rules

- Read this file before starting work each time.
- After each task is successfully completed, upload to Git (commit and push).
- LLM decisions are routed via skills in `skills/` and use `OPENAI_API_KEY` by default.
